// Mirrors the design tokens in the main site's src/app/globals.css
// (--color-ink, --color-canvas, ...) plus the gold accent used by the
// site's brand-mark shimmer, so the app reads as the same product, not a
// separate generic admin tool.
export const colors = {
  ink: '#0A0A09',
  inkMuted: '#3A3A38',
  inkFaint: '#6A6A68',
  canvas: '#F0EDE6',
  canvasWarm: '#E8E4DB',
  canvasDeep: '#D8D3C8',
  gold: '#E8C077',
  goldLight: '#F4D89A',
  error: '#9F2D20',
}
