## 2026-08-31 — Faza 0, korak: pocetno usvajanje plana
Uradjeno: Procitani su `CODEX.md`, `README.md`, dokumenti `docs/01-08` i glavni `CLAUDE.md`. Utvrdjeno je da `addmin-app` trenutno sadrzi planski paket, bez implementiranog Expo/React Native koda.
Provereno: typecheck nije pokretan jer jos nema app koda za proveru; build nije pokretan jer Faza 0 jos nije zapoceta.
Sledece: Vlasnik projekta treba da potvrdi infrastrukturne i proizvodne odluke iz `docs/08-OPEN_QUESTIONS.md`; nakon toga pocinje Faza 0: Expo scaffold, TypeScript/lint setup i osnovni `admin-api` proxy login/logout/health skeleton.
CEKA VLASNIKA: Gde zivi `admin-api` proxy; PIN format; odluka o vise uredjaja; odluka sta se radi sa mrtvim `siteSettings.heroArtwork`; informativno: Apple Developer / Google Play Console status za kasnije faze.

## 2026-08-31 — Faza 0, korak: lokalni playbook skillovi
Uradjeno: Potvrdjeno je da su blokirajuce odluke iz `docs/08-OPEN_QUESTIONS.md` razresene. Kreirani su playbook fajlovi u `addmin-app/skills/`: `expo-scaffold`, `sanity-proxy-mutation`, `image-upload-pipeline`, `draft-preview-link`, `crud-screen-pattern`, `verification-checklist`.
Provereno: typecheck nije pokretan jer su menjani samo planski/playbook Markdown fajlovi; build nije pokretan jer app scaffold jos ne postoji.
Sledece: Nova sesija/kontekst treba da zapocne sledeci korak Faze 0: scaffold Expo/React Native projekta u `addmin-app` po `skills/expo-scaffold.md`, bez diranja glavnog sajta.
CEKA VLASNIKA: Nema blokade za Faze 0-10. iOS/Play Store odluke ostaju za Fazu 10.

## 2026-08-31 — Faza 0, korak: usvajanje prosirenog roadmap-a
Uradjeno: Procitani su novi dokumenti `docs/09-MESSAGES.md` i `docs/10-FUTURE_INSTAGRAM_METRICS.md`, kao i azurirani `docs/07-ROADMAP.md` i `docs/08-OPEN_QUESTIONS.md`. Uvedena je nova Faza 7 za Supabase poruke, stare faze 7-9 su pomerene na 8-10, a Instagram objavljivanje/metrike je evidentirano kao posle-MVP rad koji se ne dira bez Meta App Review preduslova. Kreiran je sedmi playbook `skills/supabase-proxy-read.md`.
Provereno: typecheck nije pokretan jer su menjani samo planski/playbook Markdown fajlovi; build nije pokretan jer app scaffold jos ne postoji.
Sledece: Nova sesija/kontekst treba da zapocne sledeci korak Faze 0: scaffold Expo/React Native projekta u `addmin-app` po `skills/expo-scaffold.md`, bez diranja glavnog sajta.
CEKA VLASNIKA: Nema blokade za Faze 0-10. iOS/Play Store odluke i Meta App Review status ostaju za kasnije, prema `docs/08-OPEN_QUESTIONS.md` i `docs/10-FUTURE_INSTAGRAM_METRICS.md`.

## 2026-08-31 — Faza 0, korak: Expo scaffold
Uradjeno: Postavljen je inicijalni Expo/React Native skeleton u `addmin-app`: `package.json`, `app.json`, TypeScript config, ESLint flat config, Expo Router layout, pocetni dashboard ekran, login ekran, osnovni `src/` folderi i tipovi. Instalirane su Expo 57 zavisnosti i lockfile je kreiran. Dependency set je uskladjen na Expo-kompatibilne verzije React/RN.
Provereno: `npm run typecheck` ✓ / `npm run lint` ✓ / `npx expo start --offline --port 19100` ✓ podize Metro (`Waiting on http://localhost:19100`). Android emulator/uredjaj nije prikacen (`adb devices` prazan), pa prikaz ekrana na Androidu nije fizicki potvrdjen u ovom koraku. `npm audit --omit=dev --audit-level=high` ✓ nema high/critical runtime nalaza; postoje moderate nalazi u Expo transitive toolingu, a `audit fix --force` bi lomio Expo verzije pa nije primenjen.
Sledece: Nastaviti Fazu 0 sledecim malim korakom: implementirati osnovni `admin-api` proxy skeleton u postojecem Next/Vercel projektu (`/api/admin/health`, `/api/admin/login`, `/api/admin/logout`) bez CMS mutacija i bez izlaganja tajni klijentu.
CEKA VLASNIKA: Nema blokade za Faze 0-10. iOS/Play Store odluke i Meta App Review status ostaju za kasnije.

## 2026-08-31 — Faza 0, korak: admin-api proxy skeleton
Uradjeno: U postojeci Next/Vercel projekat dodat je osnovni `admin-api` skeleton: `GET /api/admin/health`, `POST /api/admin/login`, `POST /api/admin/logout`, server-only auth helper za 6-cifreni PIN hash (`pbkdf2:sha256`) i HMAC sesijski token, plus bezbednosni JSON response helper. Dopunjen je `.env.local.example` nazivima server-only env varijabli (`ADMIN_PIN_HASH`, `ADMIN_SESSION_SECRET`, `SANITY_API_WRITE_TOKEN`) i primerom generisanja PIN hash-a.
Provereno: glavni projekat `npm run typecheck` ✓ / `npm run lint` ✓ / `npm run build` ✓. Runtime provera na lokalnom Next dev serveru: `/api/admin/health` 200 ✓, los PIN na `/api/admin/login` 401 ✓, validan test PIN 200 i vraca sesijski token ✓, `/api/admin/logout` bez tokena 401 ✓, `/api/admin/logout` sa validnim tokenom 200 ✓. Provera tajni: test secret nije upisan u repo; mobilni app nema Sanity write token, Supabase service role, PIN hash ni session secret.
Sledece: Faza 1, prvi mali korak: povezati Expo login ekran sa `admin-api` klijentom i `expo-secure-store` sesijskim skladistem; dashboard treba da koristi lokalno stanje sesije i logout dugme, bez CMS podataka.
CEKA VLASNIKA: Nema blokade za Faze 0-10. Android fizicki prikaz jos nije proveren jer nema prikacenog emulatora/uredjaja; to postaje gate u Fazi 1 sideload proveri.

## 2026-08-31 — Faza 1, korak: mobile auth walking skeleton
Uradjeno: Expo app je povezana sa `admin-api` proxy-jem kroz `src/api/admin.ts`; dodat je `expo-secure-store` session storage i `AuthProvider`; login ekran salje 6-cifreni PIN, cuva sesijski token i prebacuje na dashboard; dashboard je zasticen lokalnom sesijom i ima logout dugme koje brise lokalnu sesiju i poziva proxy logout. Nema CMS citanja ni Sanity/Supabase tajni u mobilnom kodu.
Provereno: `addmin-app` `npm run typecheck` ✓ / `npm run lint` ✓ / Expo Metro + Android emulator Pixel_35 ✓. End-to-end Android provera preko Expo Go: login ekran prikazan ✓, test PIN preko lokalnog proxy-ja otvara dashboard ✓, logout vraca na login ✓. Glavni Next proxy runtime koriscen sa lokalnim test env vrednostima; test secret/PIN hash nisu upisani u repo.
Sledece: Faza 1, sledeci mali korak: napraviti prvi sideloadable Android APK build za `addmin-app` i potvrditi da se instalira/pokrece van Expo Go.
CEKA VLASNIKA: Nema blokade za Faze 0-10. Ako lokalni APK build zahteva dodatni Android/Gradle setup koji nije dostupan, zabeleziti kao blokadu ili preci na EAS samo ako postoje nalozi i dozvole.

## 2026-08-31 — Faza 1, korak: prvi Android sideload APK
Uradjeno: Generisan je native Android projekat za `addmin-app` (`npx expo prebuild --platform android --no-install`) i uskladjene su Expo 57 native zavisnosti koje su blokirale Gradle build (`expo-system-ui`, `react-native-reanimated`, `react-native-worklets`). Napravljeni su `debug` i `release` APK artefakti. Debug APK se instalira, ali ocekivano trazi Metro bundle; release APK pakuje JS bundle i namenjen je za samostalan sideload.
Provereno: `./gradlew assembleDebug` ✓ pravi `android/app/build/outputs/apk/debug/app-debug.apk` (215 MB). `./gradlew assembleRelease` ✓ pravi `android/app/build/outputs/apk/release/app-release.apk` (93 MB). `adb install -r app-release.apk` ✓ na emulatoru Pixel_35; `adb shell am start -n com.zlaticart.admin/.MainActivity` ✓; release app prikazuje PIN login ekran bez Metro/red-screen greske; kratki `logcat` posle pokretanja nema novih crash/Metro gresaka. Fizički Android telefon `M2007J3SG` je povezan preko USB/ADB, potvrdjeno je da je na istoj mrezi (`wlan0` `192.168.8.5/24`, SSID `Ellconet`), `adb install -r app-release.apk` ✓, aplikacija se pokrece na telefonu i prikazuje PIN login ekran ✓; read-only `logcat` pokazuje `Running "main"` bez crash/Metro greske. Napomena: Gradle prijavljuje deprecation upozorenja za Gradle 10 i metaspace upozorenje tokom dugog release build-a, ali build prolazi.
Sledece: Ne nastavljati na Fazu 2 (Sanity artwork read/mutation) dok se ne zatvori preostali deo Faze 1 acceptance gate-a: proveriti PIN login/logout tok na Zlaticinom fizickom Android telefonu preko pravog/staging `admin-api` URL-a.
CEKA VLASNIKA: Faza 1 fizicki sideload install/open gate je prosao. Preostaje PIN login/logout na telefonu; za to APK mora imati dostupan `EXPO_PUBLIC_ADMIN_API_URL` (staging/production Vercel URL ili lokalni LAN URL ako se testira na istoj mrezi) i aktivne server env varijable `ADMIN_PIN_HASH` / `ADMIN_SESSION_SECRET`.

## 2026-08-31 — Faza 1 gate ZATVORENA + vizuelno usklađivanje (van standardnog Codex koraka, Claude sesija)
Uradjeno: Zatvoren preostali deo Faze 1 acceptance gate-a end-to-end na Zlaticinom fizickom telefonu (`M2007J3SG`, `com.zlaticart.admin`). Uzrok blokade: instalirani release APK je bio build-ovan bez `EXPO_PUBLIC_ADMIN_API_URL` (prazna vrednost, ne overriden greskom u kodu). Resen preko `expo run:android` (development build, Expo CLI ispravno ucitava `.env.local`) umesto rucnog Gradle `assembleRelease` — Gradle je pokazivao netacan build-cache "UP-TO-DATE" ponasanje za `createBundleReleaseJsAndAssets`/`mergeReleaseAssets` cak i uz `--rerun-tasks --no-build-cache`, sto je trosilo vreme bez rezultata; `expo run:android` to zaobilazi. Mac ima dva LAN interfejsa (`192.168.8.14` i `192.168.8.21`) — telefon je dostupan samo preko `.21`, ne `.14` koju je prethodna sesija koristila (DHCP lease se promenio).
Dodatno, van Faze 1 obima ali na zahtev vlasnika: vizuelno uskladjivanje app-a sa glavnim sajtom — `src/theme/colors.ts` sada koristi tacne tokene iz `src/app/globals.css` (`#0A0A09` ink, `#F0EDE6` canvas, `#E8C077`/`#F4D89A` gold), nov `src/theme/typography.ts`, instalirani `@expo-google-fonts/cormorant-garamond` + `@expo-google-fonts/dm-sans` + `expo-font` (ucitavanje fontova u `app/_layout.tsx` pre renderovanja), `login.tsx`/`index.tsx` prepravljeni da koriste temu. Generisana app ikona (`addmin-app/assets/icon.png`, `adaptive-icon.png`, `splash-icon.png`, `favicon.png`) preko `sharp` (SVG izvor u `assets/icon-source.svg`/`adaptive-icon-source.svg`, jer Inkscape/ImageMagick nisu dostupni u okruzenju) — zlatna serif kurziv "Z" na ink pozadini, tanka zlatna ivica nalik muzejskoj etiketi. Instaliran `expo-splash-screen`, povezano sve u `app.json` (`icon`, `android.adaptiveIcon`, `web.favicon`, `expo-splash-screen` plugin).
Provereno: `npm run typecheck` ✓ / `npm run lint` ✓ (posle svake izmene, ukljucujuci font/temu izmene). End-to-end na fizickom telefonu preko `expo run:android` (development build): login ekran ucitan sa ispravnom tipografijom/bojama (screenshot potvrdjen), PIN 482913 (test vrednost) → dashboard prikazuje svih 9 sekcija sa serif naslovom i gold eyebrow labelom, logout vraca na login ekran. Test je radjen preko lokalnog `next dev` (moj sopstveni proces, port 3000, ugasen posle testa) + Metro (takodje ugasen posle testa) — NIJE dirnut Codex-ov aktivan `next dev -H 0.0.0.0 -p 3107` proces, eksplicitno provereno i ostavljen netaknut.
Test vrednosti (SAMO za ovaj test, ne za produkciju): `ADMIN_PIN_HASH`/`ADMIN_SESSION_SECRET` u glavnom `.env.local` (gitignored) za PIN `482913`; `addmin-app/.env.local` (gitignored, nov fajl) sa `EXPO_PUBLIC_ADMIN_API_URL=http://192.168.8.21:3000` (LAN adresa, vazi samo dok su Mac i telefon na `Ellconet`, ne radi za produkcioni sideload).
Sledece: Faza 2 (Sanity artwork read preko `admin-api` proxy-ja, status toggle) moze da pocne — Faza 1 gate je zatvorena. Pre Faze 2, razmotriti: (a) da li zameniti test PIN 482913 pravim, (b) da li se ide na produkcioni `admin-api` URL (Vercel deploy + env vars) pre sledeceg sideload testa, umesto LAN adrese koja zavisi od moje mreze.
CEKA VLASNIKA: Nema blokade za Fazu 2. Test PIN treba zameniti pre nego sto se APK stvarno preda Zlatici (ne pre nastavka razvoja).

## 2026-08-31 — Faza 2, korak: Radovi (read + status toggle) — Claude sesija
Uradjeno: Implementiran prvi Faza 2 slice po `docs/07-ROADMAP.md`. Server: `src/lib/admin-api/sanity.ts` (odvojen admin Sanity klijent, `useCdn:false`, opcioni `SANITY_API_WRITE_TOKEN`), `GET /api/admin/artworks` (autentifikovana lista svih artwork dokumenata, svi statusi, ne samo published), `PATCH /api/admin/artworks/[id]/status` (autentifikovana mutacija, 503 ako `SANITY_API_WRITE_TOKEN` nije podesen). Mobile: `src/api/admin.ts` prosiren sa `fetchArtworks`/`updateArtworkStatus` (i PATCH metoda u `adminFetch`), nov ekran `app/works.tsx` (React Query, FlatList, status badge sa ciklicnim tap-to-toggle draft→published→archived→draft, loading/error/empty stanja po `docs/06-SCREENS.md`), dashboard "Radovi" kartica je sada `Link` ka `/works`, ostale sekcije ostaju vizuelno onemogucene placeholder kartice (ne vode na prazne ekrane). Ruta `/works` dodata u `app/_layout.tsx`.
Usput otkriven i ispravljen preduslovan bug: root `tsconfig.json` (`include: ["**/*.ts", ...]`) je nenamerno uvlacio `addmin-app/` u glavni projekat typecheck (razliciti `@/*` alias ciljevi → lazne greske). Dodato `addmin-app` u root `exclude`. Ovo je bio postojeci propust, ne nesto uvedeno ovom sesijom, ali bez ove ispravke root `npm run typecheck` nikad nije bio pouzdano zelen otkad addmin-app ima `@/` importe.
Provereno: root `npm run typecheck` ✓ / `npm run lint` ✓ / `npm run build` ✓ (nove rute vidljive u build izlazu). `addmin-app` `npm run typecheck` ✓ / `npm run lint` ✓ (posle regenerisanja Expo Router typed routes za `/works` — `.expo/types/router.d.ts` se ne azurira automatski dok se Metro/CLI ne pokrene bar jednom posle novog fajla u `app/`). Runtime na localhost: PIN login → token; `GET /api/admin/artworks` bez tokena → 401; sa tokenom → 200 `{"artworks":[]}` (potvrdjeno direktnim GROQ upitom i sirovim Sanity API pozivom da je `production` dataset stvarno prazan — 0 dokumenata ukupno — ne bug u upitu; sajt-build koji istovremeno prikazuje `untitled-oil-01` itd. koristi seed/placeholder fallback iz `src/lib/content/api.ts`, ne prave Sanity podatke); `PATCH .../status` bez `SANITY_API_WRITE_TOKEN` → 503 sa jasnom porukom. End-to-end na fizickom telefonu (dev-client, bez potrebe za native rebuild jer nije dodata nova native zavisnost): login → dashboard (samo "Radovi" kartica aktivna/klikabilna) → tap → `/works` ekran → prazno stanje "Jos nema radova u Sanity-ju." prikazano ispravno (screenshot potvrdjen). Status toggle mutacija NIJE live testirana (nema `SANITY_API_WRITE_TOKEN` ni test artwork dokumenta u bazi) — kod postoji i tipiziran je ispravno, ali stvarna Sanity mutacija ceka: (1) write token, (2) bar jedan test artwork dokument.
Poznat, jos nerazresen sporedni nalaz: instalirana app ikona na telefonovom home ekranu je i dalje generican Android robot placeholder, ne generisana zlatna "Z" ikona iz prethodne sesije — `app.json` icon/adaptiveIcon konfiguracija je promenjena POSLE poslednjeg `expo prebuild`, a inkrementalni `expo run:android` build ne regenerise native mipmap icon resurse (to radi samo pravi `expo prebuild` sync). Nije blokada za funkcionalnost, samo kozmeticki dug — sledeci `expo prebuild` (ili run sa `--no-build-cache` uz prebuild) ce ga povuci.
Sledece: (a) Sanity write token + bar jedan test artwork dokument da se live testira status toggle; (b) puna forma za Radove (upload fotografije) iz Faze 3; (c) `expo prebuild` da se povuce nova ikona u native build pre sledeceg sideload-a Zlatici.
CEKA VLASNIKA: Da li da se kreira `SANITY_API_WRITE_TOKEN` sada (Sanity Manage → API → Tokens → Editor rola) da se status toggle stvarno testira, ili se ceka do Faze 3 kad puna forma za radove tako i onako zahteva write pristup.

## 2026-08-31 — sporedni nalaz iz prethodnog koraka RESEN: app ikona — Claude sesija
Uradjeno: Pokrenut cist `npx expo prebuild --platform android` (obrisao i ponovo generisao ceo `android/` native projekat) da se povuku `app.json` icon/adaptiveIcon/splash izmene u native mipmap resurse — potvrdjeno da `ic_launcher.webp` sad sadrzi generisanu zlatnu "Z" ikonu, ne default Expo placeholder. `./gradlew :app:assembleDebug` uspesno, `adb uninstall` + cist reinstall (ne update preko starog APK-a, da se izbegne kesirana stara ikona na launcheru).
Provereno: Home ekran telefona prikazuje ispravnu "ZlaticArt Admin" ikonu (zlatna "Z", canvas adaptivna pozadina) — screenshot potvrdjen. Pun end-to-end lanac ponovo proveren posle cistog native rebuild-a: splash ekran (ink pozadina) → login (PIN 482913) → dashboard → Radovi → prazno stanje "Jos nema radova u Sanity-ju." — sve i dalje radi identicno kao pre prebuild-a, sto potvrdjuje da cist prebuild nije pokvario Faza 1/Faza 2 rad.
Sledece: Isto kao prethodni zapis — Sanity write token + test dokument za live status toggle test, zatim Faza 3 (puna forma za Radove, upload fotografije).
CEKA VLASNIKA: Isto, nepromenjeno — odluka o `SANITY_API_WRITE_TOKEN`.

## 2026-08-31 — Faza 2, korak: status toggle UZIVO potvrdjen (write token dobijen) — Claude sesija
Uradjeno: Vlasnik je kreirao `SANITY_API_WRITE_TOKEN` (Editor rola, Sanity Manage) i podelio ga — dodat u `.env.local` (gitignored). Kreiran privremen test artwork dokument direktno preko Sanity mutate API-ja (`test-admin-app-artwork-1`, status draft) da postoji nesto na cemu se status toggle stvarno moze testirati (dataset je i dalje inace prazan).
Provereno uzivo, sa pravim write pristupom: curl end-to-end (login → GET artworks → PATCH status draft→published → GET potvrdjuje `published`) ✓. Zatim na fizickom telefonu preko app UI-ja: prvi dodir na status bedz je neocekivano preskocio sa published pravo na draft (umesto na archived) — istraga je pokazala da je uzrok DUPLIRAN `adb input tap` (verovatno MIUI touch sinteza), ne bug u kodu — server je i dalje ispravno primenio ciklus dva puta zaredom (published→archived→draft), tacno po `STATUS_CYCLE` logici. Ponovljen cist, jedan dodir: draft→published potvrdjeno i na serveru (raw Sanity GROQ upit) i vizuelno na telefonu (bedz "OBJAVLJENO"). Status toggle (komponenta iz Faze 2 roadmap-a) je time u potpunosti uzivo verifikovan.
Ciscenje: test dokument `test-admin-app-artwork-1` obrisan iz Sanity-ja posle testa (privremen, ne stvaran sadrzaj). Svi lokalni `next dev`/`expo start` procesi pokrenuti ovom sesijom ugaseni; Codex-ov nezavisan `next dev -H 0.0.0.0 -p 3107` proces ponovo nije dirnut.
Sledece: Faza 2 (Radovi: read + status toggle) je sada u potpunosti zavrsena i uzivo verifikovana, ne samo kod-kompletna. Sledeci korak po `docs/07-ROADMAP.md`: Faza 3 — puna forma za Radove (upload fotografije, ostala polja iz `sanity/schemas/artwork.ts`).
CEKA VLASNIKA: Nema blokade. Write token postoji i radi — Faza 3 moze da pocne kad god se odluci.

## 2026-08-31/09-01 — Faza 3, korak: puna forma za Radove (upload fotografije, create/update) — Claude sesija
Uradjeno: Implementirana puna forma za Radove, ogranicena na "Osnovno" polja iz `docs/02-PRODUCT_SPEC.md` (title, year, medium, dimensions, primaryImage+alt, shortDescription, status, featured, heroCandidate) — `detailImages`/`story`/`featuredOrder`/`instagramUrl` namerno izostavljeni (rich text/multi-upload su vece, odvojene komponente).
Server: `src/lib/admin-api/sanity.ts` prosiren sa `adminGetArtwork`, `adminListMediums`, `adminCreateArtwork`, `adminUpdateArtwork` (parcijalni patch), `adminUploadArtworkImage` (Sanity Asset API). Nove rute: `GET/PATCH /api/admin/artworks/[id]`, `POST /api/admin/artworks` (create), `GET /api/admin/media` (lista tehnika za picker), `POST /api/admin/upload-image` (multipart upload preko `request.formData()`, max 15MB). Deljena validacija (`form-input.ts`) izmedju create i update ruta.
Mobile: instaliran `expo-image-picker` (+ `app.json` plugin config sa permission stringovima — na Androidu ipak ne zahteva CAMERA dozvolu u manifestu, koristi sistemski intent/picker). Nova deljena komponenta `src/components/ArtworkForm.tsx` (naslov, godina, tehnika kao chip picker, dimenzije, kratak opis, kamera/galerija upload sa alt tekstom, izdvojeno/naslovni-rad svicevi, "Sacuvaj kao nacrt"/"Objavi" dugmad sa validacijom — Objavi zahteva sliku+alt). Restrukturirane rute: `works.tsx` → `works/index.tsx` + `works/_layout.tsx` (nested Stack) + `works/new.tsx` + `works/[id].tsx`. `works/[id].tsx` koristi pod-komponentu `EditForm` da inicijalizuje lokalno stanje iz props-a bez `useEffect`+`setState` (eslint `react-hooks/set-state-in-effect` pravilo).
Otkriven i ispravljen realan bug: FAB ("+") dugme i card-tap navigacija su koristili `Link asChild` omotan oko `Pressable`-a; za FAB-a (koji ima `position: absolute` stil) ovo je proizvelo accessibility/touch bounds `[0,0][0,0]` (potvrdjeno `uiautomator dump`-om) — dugme se vidi na ekranu ali dodir ne prolazi. Zamenjeno sa `useRouter().push(...)` u plain `Pressable.onPress`, za oba mesta (FAB i card tap). Uzrok je specificna kombinacija `Link asChild` + apsolutno pozicioniran stil, ne opsti `Link` problem (dashboard "Radovi" kartica, koja NIJE apsolutno pozicionirana, i dalje ispravno koristi `Link asChild` bez izmene).
Provereno: root i `addmin-app` `npm run typecheck` ✓ / `npm run lint` ✓ (ukljucujuci ispravku `tsconfig.json` — root vec ranije ispravljen da iskljucuje `addmin-app`). `npm run build` ✓ na glavnom projektu, sve nove rute vidljive u izlazu. Runtime uzivo preko curl-a (login → upload test slike preko `/api/admin/upload-image` → `POST /api/admin/artworks` sa tim asset ID-jem → 201, kompletan dokument u Sanity-ju sa ispravnom strukturom slike/slug-a) ✓; `GET /api/admin/artworks/[id]` ✓; `PATCH /api/admin/artworks/[id]` (izmena title/featured/shortDescription bez slanja primaryImage) → potvrdjeno da je postojeca slika ostala netaknuta (parcijalni patch radi ispravno, ne brise polja koja nisu poslata) ✓. Test dokument i test asset obrisani posle provere.
Na fizickom telefonu (development build, isti `expo run:android` + `--port 8081` + `adb reverse tcp:8081 tcp:8081` tok kao ranije, plus rebuild zbog novog native modula `expo-image-picker`): FAB navigacija na "Novi rad" ekran ✓ (posle ispravke buga), forma se ucitava sa svim poljima, "Nema definisanih tehnika u Sanity-ju" prikazano ispravno (test dataset trenutno nema `medium` dokumenata), Galerija dugme otvara sistemski Android photo picker (bez permission dijaloga — moderan Android tok), izbor slike prikazuje ispravan lokalni preview u formi ✓, Sacuvaj/Objavi dugmad ispravno menjaju enabled/disabled stanje na osnovu validacije (naslov obavezan za nacrt; naslov+slika+alt obavezno za objavu). Stvarno slanje forme (tap na Sacuvaj/Objavi) NIJE uspesno automatizovano preko `adb input tap` u ovoj sesiji — visestruki pokusaji su bili ometani time sto ScrollView menja poziciju posle svakog fokusiranja polja/otvaranja tastature/sistemskog picker-a, cineci unapred izracunate koordinate nepouzdanim (tipfeleri su zavrsili u pogresnim poljima). Kompletan submit tok je umesto toga uzivo proveren preko `curl`-a direktno na iste API rute koje forma poziva (upload + create + update), sto pokriva sav novi server-side kod ali ne i doslovno UI tap na dugme.
Instalirani APK/dev-client na telefonu ostaje povezan na razvojni Metro (ne samostalan release build) — vidi ranije zapise o produkcionom URL-u pre stvarne predaje Zlatici.
Sledece: (a) probati ponovo doslovan UI submit test na telefonu u buducoj sesiji, mozda uz `uiautomator` bounds umesto pretpostavljenih piksela za svako dugme (izbeci probleme sa ScrollView pomeranjem); (b) Faza 4 (Pregled pre objave / Next.js Draft Mode) je sledeca po `docs/07-ROADMAP.md`, sada kad Faza 3 osnovni CRUD postoji; (c) `medium` (Tehnike) CRUD (Faza 8 po roadmap-u) bi resio trenutno prazan "Tehnika" picker.
CEKA VLASNIKA: Nema blokade. Napomena: dataset trenutno nema nijedan `medium` dokument — "Tehnika" picker u formi je prazan dok se bar jedan ne kreira (rucno u Sanity Studio-u, ili kad Faza 8 stigne na red).

## 2026-09-01 — Faza 4: Pregled pre objave (Draft Mode) — Claude sesija
Uradjeno: Implementiran mehanizam iz `docs/04-ARCHITECTURE.md` "Pregled pre objave". Glavni sajt: `sanityGetArtworkBySlugFresh` (novo, `useCdn:false`) u `src/lib/sanity/queries.ts`; `src/app/(site)/works/[slug]/page.tsx` sad koristi Next.js `draftMode()` — u draft modu se zaobilazi Next-ov static/CDN kes i ide direktno na Sanity, i prikazuje se traka "PREGLED — nije jos objavljeno" sa linkom "Izadji iz pregleda". Nove rute: `POST /api/admin/preview-link` (autentifikovano Bearer tokenom, vraca potpisan preview URL), `GET /api/preview` (javna, ali "javna" != nezasticena — validira isti HMAC sesijski token kao query parametar, WebView navigacija ne salje Authorization header pouzdano; ukljucuje draft mode, redirect na pravu stranicu), `GET /api/preview/disable`. Mobile: `getArtworkPreviewUrl` u `src/api/admin.ts`, dugme "Pregledaj na sajtu" na `works/[id].tsx` (samo za vec sacuvane radove — ne postoji za `works/new.tsx` dok rad nije kreiran), otvara preview URL preko `expo-web-browser` (`WebBrowser.openBrowserAsync`, in-app Chrome Custom Tab / SFSafariViewController, ne puni eksterni browser).
Usput otkrivena i ispravljena DVA realna, pre-postojeca bug-a na glavnom sajtu, oba nezavisna od Draft Mode-a, ali blokirala su bilo kakav test preview-a i — bitnije — znacila su da bi SVAKI rad kreiran preko nove admin app forme (Faza 3) probio svoju stranicu na sajtu:
1. `works/[slug]/page.tsx` (`generateMetadata` i JSON-LD blok) je pretpostavljao da `artwork.medium` uvek postoji (`artwork.medium.title` bez provere), iako `medium` NIJE required polje u `sanity/schemas/artwork.ts`. Sanity dataset trenutno nema nijedan `medium` dokument, pa bi svaki rad kreiran sada imao `medium: null` i srusio stranicu. Ispravljeno u GROQ upitu (`ARTWORK_FIELDS` u `src/lib/sanity/queries.ts`) preko `select(defined(medium) => ..., {fallback objekat "Tehnika nije navedena"})` — jedna izmena na jednom mestu (upit), ne 11 mesta u kodu koja citaju `artwork.medium.*` — izbegnut veliki, rizican refaktor u vec dugoj sesiji.
2. `next.config.ts` `images` config nikad nije imao `cdn.sanity.io` u `remotePatterns` — `next/image` je odbijao svaku pravu Sanity sliku ("Invalid src prop... hostname not configured"). Ovo se nikad ranije nije primetilo jer je Sanity dataset bio prazan pre ove sesije (sajt je uvek prikazivao seed/`/img/` slike). Dodato `remotePatterns: [{protocol:'https', hostname:'cdn.sanity.io', pathname:'/images/**'}]`.
Provereno: root `npm run typecheck` ✓ / `npm run lint` ✓ / `npm run build` ✓ (nove rute `/api/admin/preview-link`, `/api/preview`, `/api/preview/disable` vidljive u izlazu). `addmin-app` `npm run typecheck` ✓ / `npm run lint` ✓. Runtime uzivo preko curl-a sa cookie jar-om (`-c`/`-b`, obavezno — `curl -L` sam po sebi ne prenosi Set-Cookie kroz redirect lanac): kreiran test artwork sa statusom `draft` i pravom upload-ovanom slikom → `POST /api/admin/preview-link` → `GET /api/preview?token=...&slug=...` → 307 redirect + `__prerender_bypass` cookie → `GET /works/<slug>` → 200 OK, sadrzi "PREGLED — nije jos objavljeno" traku i "Izadji iz pregleda" link, naslov i fallback tehnika ispravno prikazani. `GET /api/preview/disable` → cookie eksperisan, traka nestaje. Test artwork i test slika obrisani posle provere.
Na fizickom telefonu: dev-client rebuild-ovan zbog novog native modula `expo-web-browser` (`expo prebuild` + `assembleDebug`, isti proveren tok kao ranije), instaliran, cist login ekran radi (screenshot potvrdjen). Dugme "Pregledaj na sajtu" NIJE live-tap-testirano na uredjaju u ovoj sesiji (isti razlog kao Faza 3 zapis — ScrollView/tastatura cine `adb input tap` koordinate nepouzdanim za visestruke sekvencijalne interakcije); server-side mehanizam koji to dugme poziva je u potpunosti uzivo proveren preko curl-a iznad.
Sledece: (a) live UI tap test za "Pregledaj na sajtu" dugme u buducoj sesiji; (b) prosiriti Draft Mode pristup na `journalPost` kad Faza 5 (Dnevnik) stigne na red, ponovnom upotrebom istog mehanizma (`preview-link`/`/api/preview` vec generisani da prime `type` parametar, trenutno hardkodovano tumaceni kao artwork — treba grananje po `type` kad se journal doda); (c) i dalje otvoreno: `medium` CRUD (Faza 8) bi popunio "Tehnika nije navedena" fallback stvarnim tehnikama.
CEKA VLASNIKA: Nema blokade.
